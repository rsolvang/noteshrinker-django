# Django 5.2 - Latest stable version
# Note: Django 5.0+ requires Python 3.10+, tested with Python 3.13
Django>=5.2,<5.3

# Image processing
Pillow>=10.0.0

# Scientific computing - using NumPy 1.x for stability
# Note: NumPy 2.0+ has breaking changes, test before upgrading
numpy>=1.24.0,<2.0
scipy>=1.10.0
